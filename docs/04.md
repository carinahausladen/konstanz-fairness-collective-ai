---
layout: default
title: "Week 4 — Social Choice Theory for LLM Alignment"
permalink: /week4-social-choice/
---

**Paper tags.**

| Tag | Paper                                                            | Role      |
| --- | ---------------------------------------------------------------- | --------- |
| 🟦  | **Gabriel 2020** — *AI, Values, and Alignment*                   | presented |
| 🟩  | **Conitzer 2024** — *Social Choice Should Guide AI Alignment*    | reading   |
| 🟧  | **Kirk 2024** — *PRISM Alignment Dataset*                        | presented |
| 🟥  | **Zhang 2026** — *Community Alignment Dataset*                   | reading   |

---

The thread this week: **what RLHF silently does to the question of "whose values" — and why that question is, formally, a social-choice problem.**

---

## 1. The normative starting point — alignment with *whose* values?

Before any technique, the alignment program inherits an unresolved question from political philosophy: an AI system can be aligned to many different targets, and the choice among them is normative, not technical.

**🟦 Gabriel** lays out a ladder of six increasingly demanding targets:

1. **Instructions** — literal task commands.
2. **Expressed intentions** — what the user actually wanted.
3. **Revealed preferences** — what behaviour implies about desires.
4. **Informed preferences / desires** — what a fully-informed version of the user would want.
5. **Interest / well-being** — what is objectively good for the user.
6. **Values** — what the user *should* value, given moral considerations.

Each rung subsumes the previous and demands more interpretive, normative work from the system designer. Climbing the ladder is not a technical refinement — it is a step toward harder questions about *whose* preferences count, *who* aggregates them, *under what procedure*, *accountable to whom*.

This is where the social-science backbone of the week starts: alignment is not a property of a model in isolation; it is a property of a *procedure* that takes many human inputs and yields one model's behaviour.

---

## 2. Social choice — the formal machinery for "many → one"

The discipline that has studied *aggregate inconsistent, irrational, heterogeneous preferences into a collective decision* for seventy years is **social choice theory**. Three building blocks worth naming explicitly:

**2.1 Arrow's impossibility theorem (1951).** No aggregation rule can simultaneously satisfy a small set of seemingly mild fairness conditions — unrestricted domain, non-dictatorship, Pareto efficiency, independence of irrelevant alternatives. Every voting rule must trade off at least one. *There is no neutral aggregation.*

**2.2 Pairwise vs cardinal methods.** Classical aggregation rules — Borda (sum of ranks), Condorcet / Kemeny–Young (majority pairwise winner), Bradley–Terry–Luce (probabilistic model over pairwise comparisons) — pick different winners on the same ballot set. The choice of method is itself a normative act.

**2.3 The aggregation step is load-bearing.** Whatever you pick, you have implicitly chosen which fairness desideratum to drop and which subgroup will be silenced when preferences conflict. The aggregation is not a neutral pre-processing step; it is the place where the political question lives.

**2.4 Three technical terms used below.**

- **Bradley–Terry–Luce (BTL).** A probabilistic model for paired comparisons (Bradley & Terry 1952; Luce 1959). Each alternative $i$ carries a latent scalar **score** $s_i$, and the probability that $i$ is preferred to $j$ is

  $$P(i \succ j) \;=\; \frac{e^{s_i}}{e^{s_i}+e^{s_j}}.$$

  Given a dataset of observed pairwise preferences, the scores are fit by maximum likelihood. **The BTL scores are interval-scaled and identified only up to an additive constant** — only *differences* $s_i - s_j$ are meaningful, not absolute levels. Aggregation rule: the BTL winner is the alternative with the highest fitted score.
- **RLHF — Reinforcement Learning from Human Feedback.** Three stages: (1) pretrain a base language model on web text; (2) collect human pairwise preferences over model outputs and fit a **reward model** — *which is structurally a Bradley–Terry model whose scalar output is the reward*; (3) fine-tune the base model with reinforcement learning to maximise that reward. The aggregation from many humans into one model lives in stage (2).
- **Constitutional AI (CAI).** Anthropic's variant of RLHF: replace step-(2) human labels with **model-generated** labels guided by a written **constitution** — a list of principles the model uses to critique and revise its own outputs. The pairwise comparisons are now between the original output and its constitution-guided revision. The aggregation problem doesn't disappear; it moves *up one level* — whose principles enter the constitution, and how are conflicts among them resolved?

**🟩 Conitzer et al.** make the bridge to LLMs explicit: **RLHF is a voting rule in disguise.** The standard pipeline:

- collects pairwise preference comparisons,
- fits a Bradley–Terry–Luce reward model,
- optimizes the policy against that reward.

That is one specific aggregation rule (BTL over pairwise data), with all the trade-offs Arrow guarantees. [Mishra (2023)](https://arxiv.org/abs/2310.16048) — *AI Alignment and Social Choice: Fundamental Limitations and Policy Implications* — applies Arrow's theorem directly to RLHF, formally treating the reward-modelling step as a social welfare function and inheriting the impossibility consequences. Constitutional AI doesn't escape either; it just pushes the aggregation problem one level up (how is the constitution itself written, and how are its principles aggregated?).

---

## 3. From theory to data — what does heterogeneous human feedback actually look like?

Once we accept that aggregation matters, the next question is empirical: how heterogeneous *are* human preferences over LLM outputs, and can current alignment datasets capture that heterogeneity? Two open datasets — both designed to *expose* disagreement rather than average it away — let us answer this concretely. PRISM is the smaller, richer-per-participant resource and the natural starting point for project work; Community Alignment is the multilingual scale-up.

### 3.1 PRISM (Kirk) — Participatory · Representative · Individualised 🟧


| Layer | Granularity | Rows | Key columns | What it lets you analyse |
| --- | --- | --- | --- | --- |
| **Survey** | per participant | 1,500 | demographics, **system string** (text), **values description** (text), **stated importance** of 7 behaviour attributes (0–100) | who the rater is, *before* any LLM contact |
| **Conversations** | per conversation | 8,011 | participant ID, **conversation type** (unguided 38.9% / values-guided 30.7% / controversy-guided 30.4%), opening prompt, free-text post-conversation feedback | what was discussed and how it went |
| **Utterances** | per (turn × response) | 68,371 | model ID, response text, **0–100 cardinal rating**, **performance attributes** (7), **choice attributes** (7) | the rating data itself |

Three features worth highlighting before designing any analysis:

- **Cardinal feedback.** Every response is rated on a 0–100 visual analogue slider ("Terrible" to "Perfect"), and the slider position is recorded as a continuous score. Participants are *not shown the number* to avoid anchoring. Cardinal ratings can be converted to rankings; the reverse cannot. **This is what makes PRISM re-aggregable** — Borda, Kemeny–Young, BTL all live in the same data.
- **21 LLMs**, drawn from 6 providers (12 commercial-API, 9 open-access). Each participant sees ~14 of them on average; each model is rated by ~924 unique raters across ~1,430 conversations.


### 3.2 Zhang — Community Alignment, the multilingual scale-up 🟥

Two-part study across 5 countries (US, France, Italy, Brazil, India) and 5 languages (English, French, Italian, Portuguese, Hindi):

- A nationally-representative **survey** (N = 15,000) demonstrating that human preferences vary substantially across cultures while all 21 evaluated LLMs cluster tightly together.
- A new **preference dataset** with 233,319 comparisons from 3,603 unique annotators (44% with free-text explanations), built using the sampling technique below.

Two contributions beyond PRISM:

- **Empirical demonstration of algorithmic monoculture.** On the Inglehart–Welzel cultural map, humans vary substantially across cultures; all 21 evaluated LLMs cluster tightly together and align with only **41%** of human preferences globally.
- **Negatively-correlated (NC) sampling.** Preference datasets are limited not only by *who annotates*, but by *which candidate responses* the annotator gets to rate. If the candidates are themselves homogeneous, no annotator-side diversity can recover heterogeneous values. Zhang et al. propose generating candidates intentionally spread along value dimensions, so the resulting preferences carry information about value variation rather than stylistic preference.


---

## 6. Summary

1. **There is no neutral aggregation.** Every RLHF / DPO / CAI pipeline embeds a specific voting rule with specific Arrow-style trade-offs, whether or not its designers intended that.
2. **"Whose values" is not rhetorical.** Gabriel's ladder shows the question is built in to the alignment target itself, and the answer requires political philosophy, not just engineering.
3. **Pluralistic alignment needs both pluralistic annotators and pluralistic candidates.** PRISM addresses the first axis; Community Alignment shows the second is independent and equally load-bearing.
