.DS_Store
docs/_site/
_site/
.jekyll-cache/

# API keys and other secrets — never commit
notebooks/lynch_agentic_misalignment/.env
.env.local
*.env

# PDFs and internal notes — papers folder is local-only
papers/
.idea/

# Internal organisation — names, enrollment, presenter roster
orga/

# Local-only working material — not on GitHub
scripts/
slides/
